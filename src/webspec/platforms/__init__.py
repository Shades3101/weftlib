"""Per-platform request builders and response parsers.

Each module here answers two questions and nothing else: *which request would
retrieve this?* and *what does this response mean?* Executing the request is
the caller's job.
"""
